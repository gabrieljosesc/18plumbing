-- NOT YET APPLIED to the live project.
--
-- The site no longer offers quotes — membership signup replaced the quote form,
-- so this table is dead. It was verified empty (0 rows) before this was written.
-- Run it from the Supabase dashboard SQL editor when you are ready:
--
--   Supabase → SQL Editor → paste → Run

drop table if exists public.quote_requests;
